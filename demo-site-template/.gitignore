# Build results
[Bb]in/
[Oo]bj/

# JSON schema files Umbraco generates for editor tooling
appsettings-schema.json
appsettings-schema.*.json
umbraco-package-schema.json

# Local appsettings (DB connection strings, secrets)
appsettings.local.json
appsettings.Local.json

# Umbraco generates wwwroot/ (static assets) and runtime data under
# umbraco/ on boot — neither belongs in git.
/umbraco/
/wwwroot/
